import { BicicletaFilterPipe } from './bicicleta-filter.pipe';

describe('BicicletaFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BicicletaFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
